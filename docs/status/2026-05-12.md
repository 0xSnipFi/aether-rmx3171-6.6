# Status snapshot — 2026-05-12

## What ships today

### v6 modem-buildproof artifact
- `releases/AETHER_RMX3171_6.6_MT6768-20260512v6-modem-buildproof.zip`
- 59 MB, sha256 `bdf0f688d165ddc276a4df722c14e564f22969c67d55e3d4238121f38fc8bb78`
- `dtbo.img` sha256 `7cf2a48edd7d8c01c9f5987434e19475f0798ba42308b9a424a5ef1274b41e5c`
- Linux 6.6 Samsung/AETHER base with Android 16 board config
- Image: 26.99 MB raw, 11.98 MB gz, 12.17 MB gz-dtb
- 107 in-tree .ko modules + 10 external MTK modem .ko modules
- Release zip contains `Image.gz-dtb`, `dtbo.img`, `system_dlkm`, `vendor_boot`, and `vendor_dlkm` module sets

### Earlier releases
- v1: `AETHER_RMX3171_6.6_A16-20260512.zip` (58 MB) — first build
- v2: `..._6.6_MT6768-20260512v2.zip` (102 MB) — MTK enable overlay
- v3: `..._6.6_MT6768-20260512v3.zip` (97 MB) — config refinement
- v4: `..._6.6_MT6768-20260512v4.zip` (99 MB) — broad MTK symbol build
- v5: `..._6.6_MT6768-20260512v5-aether-rmx3171-a16.zip` (53 MB) — A16 board/release cleanup
- 4.14 legacy: `AETHER_RMX3171_4.14_legacy-20260511.zip` (21 MB) — daily-use track

## Real port progress

| Driver | Status | Lines |
|---|---|---:|
| `pinctrl-mt6768` | ✅ ported, in vmlinux (5 API fixes from 4.14) | 88 + 2750 |
| connsys gen4m WiFi | ✅ ported, 3 .ko built (work via aetherx tree) | n/a |
| `btmtk` + `btmtkuart` + `btmtksdio` | ✅ mainline 6.6 (.ko) | n/a |
| `mtk-sd` (eMMC) | ✅ mainline 6.6, built-in | n/a |
| `mt6358` PMIC | ✅ mainline 6.6, built-in | n/a |
| `mt6370` charger | ✅ mainline 6.6 (.ko) | n/a |
| MTK DRM display framework | ✅ mainline 6.6, built-in | n/a |
| MTK IOMMU | ✅ mainline 6.6, built-in | n/a |
| MTK SCP firmware loader | ✅ mainline 6.6 (.ko) | n/a |
| `xhci-mtk` USB host | ✅ mainline 6.6, built-in | n/a |
| `wireguard` | ✅ NetHunter VPN | n/a |
| KernelSU | ✅ built-in (CONFIG_KSU=y) | n/a |
| `panel-ilt9881h-rmx3171` | ✅ builds as AETHER .ko, device-test pending | slim port |
| `nt36525b-rmx3171` | ✅ builds as AETHER .ko, touch hardware-test pending | slim port |
| `sia81xx-aether` | ✅ builds as AETHER .ko, audio route-test pending | slim port |
| `aether-simple-gauge` | ✅ builds as AETHER .ko, SOC accuracy-test pending | slim port |
| `goodix-fp-rmx3171` | ✅ builds as AETHER .ko, HAL/device-test pending | slim port |
| `mt6370-pe-rmx3171` | ✅ builds as AETHER .ko, charging-test pending | slim port |
| `fm-mt6631-aether` | ✅ builds as AETHER .ko, connsys/device-test pending | slim port |
| ECCCI modem stack | ✅ 10 external .ko build and package, SIM/RIL test pending | 6.6 MTK source |

**Total**: 20 driver subsystems now compile/package in the A16 release path.

## Staged-but-not-ported

| Driver | Source size | Strategy doc |
|---|---|---|
| `clk-mt6768` | 6500 LoC | `ports/TODO/clk-mt6768/README.md` |
| full `gm30-battery` | 15000 LoC | simple-gauge fallback builds; full coulomb counter not ported |
| full MTK ISP3 camera | ~27 MB source | not ported; 6.6 mtkcam is ISP7/8 and sensor-mismatched |
| Mali G52 Bifrost DDK | ~28 MB source | not ported; 6.6 staged `mali-r49p1` is not the right RMX3171 generation |

**Total staged**: multiple 4.14/6.6 sources remain for hard ports; camera/GPU are the largest unsolved pieces.

## Repo health

```
tracked files:      281 (after restructure)
untracked:          0
ignored:            123069 (kernel-6.6 + device-modules + vendor-modules + KSU + AnyKernel + out)
git commits:        8 on main
contributors:       1 (dipu)
```

Top-level structure clean: only `README.md`, `CONTRIBUTING.md`, `RELEASES.md`,
`LICENSE`. No log files at root.

## What's NEW this snapshot (since 2026-05-11)

1. Added `aether-rmx3171/ports/TODO/` with 7 port-task scaffolds + master
   `README.md` containing 4.14→6.6 API cheatsheet.
2. Staged 2 MB of 4.14 source for community porting (gm30/sia81xx/panel/
   fm/goodix).
3. Deep-audited Samsung tree: confirmed `mtkcam` ISP8 vs RMX3171 ISP3
   sensor mismatch (0% overlap) — camera realistically infeasible.
4. Confirmed `gpu/mali_avalon/mali-r49p1` is Valhall, RMX3171 G52 = Bifrost
   — wrong driver generation.
5. Confirmed `connectivity/wlan/core/gen4m+gen4m_ext+gen4m_s1` is **superset**
   of our current aetherx WiFi port — community can switch to this for fuller
   bt+fm+gps+wlan coverage.
6. Restructured repo: `.build-logs/` (gitignored), `docs/{STRUCTURE,MISSING,
   A16_BRINGUP,VENDOR_BLOBS}.md`, `aether-rmx3171/{abi,firmware}/` placeholders,
   `scripts/{build,release}/`.
7. Deleted stale: `AETHER_6.6_STATUS.md`, `VENDOR_TREE_STATUS.md`.
8. Added A16 release plumbing for `system_dlkm`, `vendor_boot`, `dtbo.img`,
   bootconfig, and manifest-validated module packaging.
9. Added AETHER MRDUMP compatibility export so MTK modem modules can link
   without the full MTK AEE/IPANIC stack.
10. Build-hooked MTK ECCCI modem modules:
   `rps_perf`, `ccci_util_lib`, `ccmni`, `ccci_auxadc`, `ccci_fsm_scp`,
   `ccci_ccif`, `ccci_dpmaif`, `ccci_cldma`, `ccci_md_all`, `cpif`.

## What's BROKEN / unknown

- **No physical device boot test yet.** All claims = "compiles cleanly", not
  "runs cleanly".
- Display panel driver now builds, but panel timings/backlight need physical
  RMX3171 boot test.
- Touch, audio PA, battery gauge, charging PE+, fingerprint, and FM all build
  as AETHER modules but still need hardware validation.
- Cellular kernel modules now build/package, but RIL is not claimed working
  until modem DTS reserved memory, firmware blobs, SELinux, and SIM tests pass.
- Camera full ISP and Mali G52 full 3D are still not solved.

## Honest claims

| Track | Claim |
|---|---|
| 4.14 legacy | flashable, has working full MTK BSP (634 sym), KSU + NetHunter — should boot |
| 6.6 v6 | compiles/packages clean, includes AETHER visible-hardware modules + ECCCI modem modules — **untested on device** |

Don't claim "production daily-driver" for 6.6 until at least P0 + P1 items
done **and** community device-test confirms boot.

## Next actions (no device required)

1. Add RMX3171 modem reserved-memory and ECCCI nodes to DTS, then verify
   `/dev/ccci*` creation on device.
2. Start Mali G52 Bifrost r25/r34/r38 port from 4.14; keep Vulkan claim at
   stock Mali userspace capability, realistically Vulkan 1.1 class.
3. Start full MTK ISP3 camera port or a limited V4L2 raw-sensor fallback.
4. Fix remaining DTBO warnings from inherited Samsung/MTK includes.
5. Replace clang-14 with Android clang-r510928/clang-18 for production builds.

## Next actions (device required)

6. Community flash + post boot log via GitHub Issue.
7. Iterate panel/touch/audio DTS overrides against probe failures.
